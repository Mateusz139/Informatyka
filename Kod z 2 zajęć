#include <iostream> 
 
 
int main() { 
 
 
    int number1 = 1; 
    int number2 = 1; 
    int *ptr=&number1; 
    int *ptr1=&number2; 
    do { 
        printf("Podaj pierwsza liczbe: \n"); 
        scanf_s("%d", ptr); 
        printf("Podaj druga liczbe: \n"); 
        scanf_s("%d", ptr1); 
 
            printf("%d+%d = %d \n", *ptr, *ptr1, *ptr + *ptr1); 
            printf("%d-%d = %d \n", *ptr, *ptr1, *ptr - *ptr1); 
            printf("%d*%d = %d \n", *ptr, *ptr1, *ptr * *ptr1); 
            printf("%d/%d = %d \n", *ptr, *ptr1, *ptr / *ptr1); 
            printf("%d modulo %d = %d \n", *ptr, *ptr1, *ptr % *ptr1); 
        } 
        while (*ptr != 0 || *ptr1 != 0); 
    } 

#include <iostream> 
#define SIZE 9 
 
int main() { 
    printf("Tablice \n"); 
    int numbers2[SIZE]; 
    int numbers[SIZE]={1,55,3,40,-5,-132,7,23,37}; 
 
//     for(int i=0; i<SIZE; i++){ 
//         printf("numbers[%d]=%d\n",i, numbers[i]); 
// 
//     } 
    for(int i=0; i<SIZE; i++){ 
         printf("*(numbers + %d)=%d\n",i, *(numbers+i)); 
 
    } 
 
     int maximum =*numbers; 
     for(int i=0; i<SIZE; i++){ 
         if(maximum<*(numbers +i)) 
         { 
             maximum=*(numbers+i); 
         } 
     } 
     printf("max=%d\n",maximum); 
 
     int min =*numbers; 
     for(int i=0; i<SIZE; i++){ 
         if(min>*(numbers+i)) 
         { 
             min=*(numbers+i); 
         } 
     } 
     printf("min=%d\n",min); 
 
     int sum=0; 
     for(int i=0; i<SIZE; i++) { 
         sum+=*(numbers+i); 
     } 
     float average = (float)sum/SIZE; 
     printf("average=%.2f\n",average); 
 
    int a=0; 
    for(int i=0; i<SIZE; i++) { 
        printf("podaj %d liczbe tablicy",i); 
        scanf_s("%d",&a); 
        *(numbers2+i)=a; 
 
    } 
    for(int i=0; i<SIZE; i++){ 
        printf("*(numbers2+%d)=%d\n",i, *(numbers2+i)); 
return 0;
    } 
 
 
 
        for(int i=0;i<8;i++) { 
        int temp; 
        if(*(numbers+i)>*(numbers+(i+1))) { 
            temp=*(numbers+(i+1)); 
            *(numbers+(i+1))=*(numbers+i); 
            *(numbers+i)=temp; 
            i=-1; 
        } 
 
    } 
    printf("posortowane liczby: "); 
    for(int i=0;i<9;i++) 
    { 
        printf("%d, ",*(numbers+i)); 
    } 
    printf("\n"); 
    printf("Wylicznie mediany\n"); 
    float median=0; 
    if (SIZE % 2 == 0) { 
    median=(float)(*(numbers+SIZE/2)+*(numbers+SIZE/2-1))/2; 
    printf("mediana=%f\n",median); 
    } 
    else { 
        median=*(numbers+(SIZE-1)/2); 
        printf("mediana=%f\n",median); 
    } 
 
 
    return 0; 
} 
 
 

 
